
Lists {

    maximum = \list(
        collect list \recursive(\element(max element recursive))
    );

    find = \list(\element({
        first = index list 0;
        others = filter list \ele(not (eq ele first));

        = if (eq element first)
            0
            [add 1 (Lists.find others element)];
    }));

}

test = Lists.find (range 2 10) 4;